# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/tunelar_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
# Update this to your actual MySQL root password
spring.datasource.password=sauced_and_fried_04
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate Configuration - using the recommended dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Server Configuration
server.port=8080

# File Upload Configuration
spring.servlet.multipart.max-file-size=20MB
spring.servlet.multipart.max-request-size=20MB

# JWT Configuration
app.jwt-secret=your_jwt_secret_key_should_be_very_long_and_secure
app.jwt-expiration-milliseconds=86400000

# Application name
spring.application.name=backend